# ==================== CONFIGURAÇÃO DE AMBIENTE ====================
# COPIE ESTE ARQUIVO PARA .env E PREENCHA COM VALORES REAIS
# Comando: cp .env.example .env (Linux/Mac) ou copy .env.example .env (Windows)

# ==================== AMBIENTE ====================
# Valores: development | production
FLASK_ENV=development

# ==================== SEGURANÇA ====================
# CRÍTICO: Gere uma chave secreta forte para produção
# Comando: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=sua-chave-super-secreta-aqui-muito-longa-64-caracteres-minimo

# ==================== USUÁRIO ADMIN ====================
# Senha do primeiro usuário admin (usado em flask seed-db e setup_railway.py)
ADMIN_PASSWORD=SenhaSuperForte123!

# Email do admin (opcional - usado para recuperação)
ADMIN_EMAIL=seu-email@exemplo.com

# ==================== BANCO DE DADOS ====================
# Para desenvolvimento local (SQLite)
DATABASE_URL=sqlite:///site.db

# Para produção (PostgreSQL - Railway fornece automaticamente)
# DATABASE_URL=postgresql://usuario:senha@host:5432/nome_do_banco

# ==================== CONFIGURAÇÕES OPCIONAIS ====================

# Email (para envio de notificações - implementação futura)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=seu-email@gmail.com
# MAIL_PASSWORD=sua-senha-de-app-do-gmail

# Google Analytics (já configurado no template)
# GA_TRACKING_ID=G-5QWHHDMTCS

# ==================== NOTAS IMPORTANTES ====================
# 1. NUNCA commite o arquivo .env no Git (já está no .gitignore)
# 2. Use valores diferentes para development e production
# 3. SECRET_KEY deve ter no mínimo 32 caracteres aleatórios
# 4. ADMIN_PASSWORD deve ser forte: min. 8 chars, maiúscula, minúscula, número
# 5. No Railway, configure estas variáveis no painel de "Variables"